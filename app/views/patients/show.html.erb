<h2><%= @p.name %></h2>

<ul>
    <%@p.appointments.each do |a|%>
        <% t = a.appointment_datetime%>
        <h3><%= link_to(a.doctor.name, doctor_path(a.doctor))%></h3>

        <%= t.strftime("%B")%> <%= t.strftime("%d")%>, <%= t.strftime("%Y")%> at <%= t.strftime("%H")%>:<%= t.strftime("%M")%> in </p>
    <% end %>
<ul>